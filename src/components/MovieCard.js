import React from 'react';
import styled from 'styled-components';

const Card = styled.div`
  /* background-color: teal; */
`;

const MovieCard = () => {
  return <Card>MovieCard</Card>;
};

export default MovieCard;
